import { ApplicationUserInfo } from './applicationuserinfo';

describe('Applicationuserinfo', () => {
  it('should create an instance', () => {
    expect(new ApplicationUserInfo()).toBeTruthy();
  });
});
